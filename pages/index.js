export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Weapon System API</h1>
      <p>Next.js is now powering the admin and gameplay APIs. Explore endpoints under <code>/api</code>.</p>
    </main>
  );
}
